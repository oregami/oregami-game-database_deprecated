<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
	version="2.0">
    <persistence-unit name="dataHsql" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <properties>
			<!--
			properties are defined in the yml-file for dropwizard (e.g. src/test/resources/oregami_test.yml
            -->
        </properties>
    </persistence-unit>

	<persistence-unit name="dataMysql" transaction-type="RESOURCE_LOCAL">
		<provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
		<properties>
		</properties>
	</persistence-unit>

</persistence>
